// Re-export so the test tree keeps its historical import path; the implementation lives in src so
// packages outside core (and the conformance suite) can use the same harness.
export * from "@opencode-ai/core/testing/effect"
